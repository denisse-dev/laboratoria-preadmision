var phrase = prompt("Dame una frase");
var long = phrase.lenght
// Imprimir caractéres de un string en diferentes líneas, mayúsculas.
var charByChar = function(phrase)
{
    phraseVar = phrase.toUpperCase();
    console.log(phraseVar);
    
}

charByChar(phrase);

// Cuenta número de caractéres del string sin usar .lenght

// Cuenta el número de espacios del string
